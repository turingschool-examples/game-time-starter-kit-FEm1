import GamePiece from './gamePiece';

class BulletPieces {
  constructor(x, y, dy, radius) {
    this.x = x;
    this.y = y;
    this.dy = 5;
    this.radius = 80;
  }
}

export default BulletPieces; 